export type donateFormTypes = {
    amount: number,
    name: string,
    email: string,
    payment_method: string,
    phone?: number,
    country?: string,
    city?: string,
    address?: string
}